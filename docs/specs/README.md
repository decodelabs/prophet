# Prophet Specs

Status: active

Use this folder for provisional planning that has not been promoted into
architecture or contracts yet.

Current posture:

- the OpenAI strategy split lane has promoted its core Prophet contract
  decisions already
- the additive OpenRouter adapter lane is implementation work, not a breaking
  core contract rewrite
- future breaking contract cleanup for `reply()` should start here before it is
  treated as roadmap-ready

## Next Task

Keep new specs narrow and promote settled core contract changes back into
`architecture/` and `contracts/`.
